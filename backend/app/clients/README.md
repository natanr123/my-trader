Clients should be agnostic to the app settings

For example, we should not use app.core.config.alpaca_settings in MyAlpacaClient

When you want to connect client and app settings, use the api/deps folder